import { CreditcardValidatorDirectiveDirective } from './creditcard-validator.directive.directive';

describe('CreditcardValidatorDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new CreditcardValidatorDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
